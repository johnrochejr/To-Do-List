<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>To-Do List</title>
    <link rel="stylesheet" href="/screen.css">
  </head>
  <body>
    <div class="newTask">
      <h3>To-Do List</h3>
      <form action="/newTodo/{{todo}}" method="post">
        <p>What's up for today?</p>
        <input type="text" name="todo" placeholder="Tasks">
        <button type="submit" name="todo">Add</button>
      </form>
    </div>
    <div class="pendingTask">
      <h4>Pending</h4>
      <ul>
        {{#pending}}
        <li>
          <form action="/completedTasks/{{.}}" method="post">
            <button type="submit">Done</button> {{.}}
          </form>
        </li>
        {{/pending}}
      </ul>
    </div>
      <div class="completedTask">
        <h4>Done</h4>
        <ul>
          {{#completed}}
          <li>
            <form action="/notDone/{{.}}" method="post">
              <button type="submit">Undo</button> <del>{{.}}</del>
            </form>
          </li>
          {{/completed}}
        </ul>
      </div>
  </body>
</html>
